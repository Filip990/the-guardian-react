import styled from "styled-components";
import { Button } from "react-bootstrap";

export const LoadMoreBtn = styled(Button)`
  margin: 2%;
`;

export const ScrollToTopImg = styled.img`
  position: fixed;
  right: 10px;
  bottom: 34px;
  width: 3rem;
  height: initial;
`;
